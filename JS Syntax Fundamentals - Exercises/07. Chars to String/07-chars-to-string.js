function charsToString(first, second, third) {
    let result = first.concat(second, third);

    console.log(result);
}

charsToString(
    'a',
    'b',
    'c'
);

charsToString(
    '%',
    '2',
    'o'
);

charsToString(
    '1',
    '5',
    'p'
);